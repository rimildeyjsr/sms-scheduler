<h1>Saved Campaigns</h1>

<% @campaigns.each do |campaign| %>
    <p><%= link_to campaign.campaign_name, "/home/#{campaign.id}" %></p>
<%end%>

<p> <%= link_to 'Create New Campaign', "/home/new" %> </p>

